# Python
ronny=float(input())
roberto=float(input())
maria=float(input())
gasto= 400+100+80+800+95
soma= roberto+ronny+maria
resultado=soma-gasto
if soma<gasto:
	print("dinheiro insuficiente")
else:
	if soma>gasto:
		print("O que sobrou foi:" "%.2f" %resultado)	
